import Vue from 'vue'
import Vuetify from 'vuetify/lib'
import '@fortawesome/fontawesome-free/css/all.css'

Vue.use(Vuetify, {
	iconfont: 'fa',
	theme: {
		primary: '#009530' // a color that is not in the material colors palett
	}
})
